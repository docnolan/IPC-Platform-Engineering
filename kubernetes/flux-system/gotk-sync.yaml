# This file contains the GitRepository and Kustomization resources for the flux-system itself
# Generated by `flux bootstrap`
#
# NOTE: In this architecture, Flux is managed by the local Azure Arc Agent.
# It is installed via the 'microsoft.flux' extension on the VM.
# These files are placeholders/semantic references only.
